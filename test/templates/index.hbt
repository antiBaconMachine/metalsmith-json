<!DOCTYPE html>
<html>
    {{#each collections.builds}}
        <div class='build'>
            <h2>Build #{{data.build}}</h2>
            <p>version: {{data.version}}</p>
        </div>
    {{/each}}
</html>

